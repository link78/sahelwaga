{
  "name": "@sahelwaga/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end smoke tests for Sahel Pharma portal.",
  "scripts": {
    "test": "echo \"Skipping E2E in unit test runs; use 'pnpm e2e' instead.\" && exit 0",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "install:browsers": "playwright install --with-deps chromium",
    "lint": "echo \"(no lint for e2e)\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.4"
  }
}
